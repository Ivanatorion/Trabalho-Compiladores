float glob1; //Var global simples
int glob2[100]; //Var global vetor

static int glob3[200];

/* Comentario
Em multiplas
Linhas */

int main(){
   static const bool flag1 <= true;

   if (flag1 < 9) then {
      flag1 = 5 + flag1; //Inconsistencia de tipos (Sem problemas agora)
      return flag1;
   }
   else {
      flag1 = 2 - flag2; //Identificador nao declarado (Sem problemas agora)
      return !flag1;
   }

   break;
}
